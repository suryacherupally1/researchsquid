version: "3.9"

services:
  neo4j:
    image: neo4j:5.15-community
    environment:
      NEO4J_AUTH: neo4j/researchsquid
    volumes:
      - neo4j_data:/data
    healthcheck:
      test: ["CMD", "neo4j", "status"]
      interval: 10s
      retries: 5
    networks: [squid_internal]

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: squid
      POSTGRES_USER: squid
      POSTGRES_PASSWORD: researchsquid
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U squid"]
      interval: 5s
      retries: 5
    networks: [squid_internal]

  redis:
    image: redis:7-alpine
    networks: [squid_internal]

  coordinator:
    build:
      context: .
      dockerfile: coordinator/Dockerfile
    environment:
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: researchsquid
      DATABASE_URL: postgresql://squid:researchsquid@postgres:5432/squid
      REDIS_URL: redis://redis:6379
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      BRAVE_API_KEY: ${BRAVE_API_KEY}
      SQUID_API_KEY: ${SQUID_API_KEY}
    depends_on:
      neo4j: { condition: service_healthy }
      postgres: { condition: service_healthy }
    networks: [squid_internal]

  workers:
    build:
      context: .
      dockerfile: workers/Dockerfile
    environment:
      COORDINATOR_URL: http://coordinator:8000
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      BRAVE_API_KEY: ${BRAVE_API_KEY}
    depends_on: [coordinator]
    networks: [squid_internal]
    deploy:
      replicas: 2

  mcp:
    build:
      context: .
      dockerfile: coordinator/Dockerfile
    command: squid-mcp --transport sse --port 8001
    environment:
      COORDINATOR_URL: http://coordinator:8000
      SQUID_API_KEY: ${SQUID_API_KEY}
    ports:
      - "8001:8001"
    networks: [squid_internal, squid_external]

networks:
  squid_internal:
    driver: bridge
  squid_external:
    driver: bridge

volumes:
  neo4j_data:
  postgres_data:
